export type CalendarDto = {
  account_id: string;
  description: string;
  id: string;
  is_primary: boolean;
  job_status_id: string;
  location: string;
  name: string;
  object: string;
  read_only: boolean;
  timezone: string;
};
